# .streamlit/secrets.toml — Local secrets for Streamlit
# Copy this file to .streamlit/secrets.toml and replace the placeholder.
# NEVER commit secrets.toml with a real key to version control.

GROQ_API_KEY = "your-api-key-here"
